# Requirements per le simulazioni QuTiP del Framework TET-CVTL v2.0
# Protocollo RENASCENT-Q - Aprile 2026

qutip>=5.0.0          # Quantum Toolbox in Python (versione stabile recente)
numpy>=1.26.0         # Calcoli numerici ad alte prestazioni
scipy>=1.12.0         # Funzioni scientifiche e integratori
matplotlib>=3.8.0     # Generazione grafici (entropia, concurrence, etc.)
seaborn>=0.13.0       # Stile grafico migliorato (opzionale ma consigliato)

# Dipendenze opzionali utili per simulazioni grandi e parallelizzazione
joblib>=1.3.0         # Parallelizzazione semplice delle simulazioni
tqdm>=4.66.0          # Progress bar per simulazioni lunghe

# Per salvataggio dati e figure
pandas>=2.2.0         # Gestione dati (opzionale)
pillow>=10.0.0        # Manipolazione immagini (se servono post-processing)

# Per riproducibilità
pip>=23.0
setuptools>=68.0
